# Project agent notes

This project uses Next.js App Router. Read local framework docs or existing code before changing framework-level behavior.

Keep secrets out of Git. Only `.env.example` may be committed.
